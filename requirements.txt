# =============================================================================
# Top-level developer install — everything needed to run ALL tiers on a laptop.
# -----------------------------------------------------------------------------
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#
# Per-tier subsets live in:
#   pi_edge_node/requirements.txt   (edge node)
#   src/requirements.txt            (backend: API + virtual sensors)
#   laptop_dashboard/requirements.txt (analytics + GUI)
#
# NOTE: the Tkinter GUI also needs the system Tk package, which pip cannot
# install. On Debian/Ubuntu:  sudo apt install python3-tk
# =============================================================================

# --- Messaging (all tiers) ---
paho-mqtt>=2.0.0

# --- Backend: REST API (Milestone 8) ---
flask>=3.0.0
flask-restx>=1.3.0
werkzeug>=3.0.0

# --- Virtual sensors / ML (Milestone 9) ---
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=1.24.0
joblib>=1.3.0

# --- Visualization (Milestone 11) ---
matplotlib>=3.7.0
seaborn>=0.13.0

# --- Raspberry Pi GPIO (only installs on ARM; laptop falls back to --simulate) ---
gpiozero>=2.0 ; platform_machine == "armv7l" or platform_machine == "aarch64"
lgpio>=0.2 ; platform_machine == "armv7l" or platform_machine == "aarch64"
