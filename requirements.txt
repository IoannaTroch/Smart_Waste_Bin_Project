# Backend tier dependencies (Milestones 8-9)
flask>=3.0.0
flask-restx>=1.3.0
paho-mqtt>=2.0.0

# ML virtual sensor (Milestone 9)
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=1.24.0
joblib>=1.3.0

# Flask-RESTx 1.3.x works with Werkzeug 3.x but not 4.x. Bound it (not just a
# floor) so a future major Werkzeug release can't silently break the import.
werkzeug>=3.0.0,<4.0.0
