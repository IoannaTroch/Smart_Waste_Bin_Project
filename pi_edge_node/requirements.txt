# Raspberry Pi edge-node dependencies (Milestones 2-7)
#
# This tier reads real GPIO, so the GPIO stack is required. Build/run on the
# Raspberry Pi (rpi-lgpio pulls in the lgpio backend it needs on Bookworm).
paho-mqtt>=2.0.0
gpiozero>=2.0
rpi-lgpio
